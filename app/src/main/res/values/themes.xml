<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OrcamentoFacilFacil" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Theme de Despesas -->
    <style name="despesaTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:colorPrimary">@color/colorPrimaryDespesas</item>
        <item name="android:colorPrimaryDark">@color/colorDarkDespesas</item>
        <item name="android:colorAccent">@color/colorAccentDespesas</item>
    </style>

    <!-- Theme de Receitas -->
    <style name="receitaTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:colorPrimary">@color/colorPrimaryReceitas</item>
        <item name="android:colorPrimaryDark">@color/colorDarkReceitas</item>
        <item name="android:colorAccent">@color/colorAccentReceitas</item>
    </style>

    <style name="Theme.OrcamentoFacilFacil" parent="Base.Theme.OrcamentoFacilFacil" />
</resources>